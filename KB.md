# rocareer/webman-status-code（统一状态码与消息，与 radmin 同步）

> 面向用户 / 后台助手的产品级知识。开发者文档见 README / CHANGELOG / docs。

## 是什么

本插件为 webman 项目提供一套统一、可复用的业务状态码与中文提示消息，并与 rocareer/radmin 3.2+ 内置状态码完全一致。业务代码抛错时引用统一的状态码常量，即可得到规范码值和友好中文提示，前端或客户端据此统一识别错误。接入方用插件提供的一套状态码（用户不存在、密码错误、未登录等）作为统一口径即可。

## 用途与意义

解决的核心问题是错误码口径不一致：各模块、各接口若各自定义错误码，前端和客户端难以统一判别，也常出现同一含义对应多个码值的混乱。本插件把状态码收敛到一处并配好中文消息，让返回的错误既规范又易懂；同时与 radmin 同步，使全家桶口径统一，避免跨模块对接对不上号。自动生成也让新增码时自动补码排序，减少人工维护出错。

## 主要功能

- 统一状态码常量：覆盖登录校验、权限、Token、用户、校验等常见业务错误。
- 中文消息映射：每个状态码有对应中文提示，getMessage() 可取；未登记码返回`未知错误`。
- 业务异常抛码：抛业务异常时携带状态码，异常统一转成规范响应，用户看到友好提示。
- 与 radmin 同步：状态码定义与 radmin 3.2+ 完全对齐，全家桶口径统一。
- 自动生成维护：scode:run 命令扫描代码库，自动重生成状态码类文件并提示新增码。

## 关键概念

- 状态码（Status Code）：一组数字常量，标识某一类具体业务错误，如`用户不存在``密码错误`。
- 中文消息（Message）：每个状态码对应的、给用户看的提示文案；取不到时返回`未知错误`。
- 8 位错误码编号规范：错误码为 8 位负数，前 3 位系统标识、中间 2 位服务标识、后 3 位错误码。
- 已发布码只增不改：已下发的码值只增加、不修改，避免前端或客户端依赖旧值错乱。

## 使用入口

- 安装接入：composer 安装并执行插件安装，将统一状态码类复制到宿主项目即可使用。
- 代码中使用：业务代码直接引用状态码常量，抛业务异常时带上状态码，或用 getMessage() 取中文提示。
- 自动生成命令：php webman scode:run 扫描代码库，自动重生成状态码类文件并提示新增码。
- 插件配置：通过配置项管理启用开关、系统标识、新增码起始范围与扫描范围。
